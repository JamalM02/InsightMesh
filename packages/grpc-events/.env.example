HOST="0.0.0.0"
PORT=50052

# PostgreSQL (Neon)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# Kafka (URL auto-injected by ecosystem.config.js in production from root .env KAFKA_PORT)
KAFKA_URL="localhost:29092"
KAFKA_CLIENT_ID="events-service"
